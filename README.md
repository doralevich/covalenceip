# covalenceip.com

Next.js site for covalenceip.com, replacing the WordPress site.

```bash
npm install
npm run dev     # http://localhost:3000
npm run build
npm run lint
```

See `AGENTS.md` for the migration rules.
